

<script setup>
import login from './components/login.vue'
import dashboard from './components/dashboard.vue'
import sidebar from './components/sidebar.vue'
import { onMounted, ref } from 'vue'

const user = ref(null)

//write a logic to check if the user has logined in
onMounted(()=>{
  user.value = localStorage.getItem('isLoggedIn');
  console.log(user.value)
})
function handleLoginSuccess(isLoggedIn) {
  if (isLoggedIn) {
    user.value = true;
    localStorage.setItem('isLoggedIn', JSON.stringify(data));
  }
}
</script>

<template>
<login v-if="!user" @loginSuccess="handleLoginSuccess"></login>
<dashboard v-else></dashboard>
</template>


<style scoped>

/* Oyera Auto Garage — Styles */

:root {
  --color-bg: #0d0f12;
  --color-bg-elevated: #161a20;
  --color-bg-card: #1c2129;
  --color-surface: #242b35;
  --color-accent: #f59e0b;
  --color-accent-hover: #fbbf24;
  --color-accent-muted: rgba(245, 158, 11, 0.15);
  --color-text: #f1f5f9;
  --color-text-muted: #94a3b8;
  --color-border: rgba(255, 255, 255, 0.08);
  --font-display: "Bebas Neue", sans-serif;
  --font-body: "DM Sans", system-ui, sans-serif;
  --radius: 12px;
  --radius-lg: 20px;
  --shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  --transition: 0.25s ease;
}

*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-body);
  background: var(--color-bg);
  color: var(--color-text);
  line-height: 1.6;
  overflow-x: hidden;
}

img {
  max-width: 100%;
  display: block;
}

a {
  color: inherit;
  text-decoration: none;
}

ul {
  list-style: none;
}

.container {
  width: min(1140px, 92vw);
  margin-inline: auto;
}

/* Header */
.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  padding: 1rem 0;
  transition: background var(--transition), box-shadow var(--transition);
}

.header.scrolled {
  background: rgba(13, 15, 18, 0.92);
  backdrop-filter: blur(12px);
  box-shadow: 0 1px 0 var(--color-border);
}

.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2rem;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 1.25rem;
  letter-spacing: 0.02em;
}

.logo-icon-img {
  width: 2.75rem;
  height: 2.75rem;
  object-fit: contain;
  background: #fff;
  border-radius: 10px;
  padding: 3px;
  flex-shrink: 0;
}

.logo-text strong {
  color: var(--color-accent);
  font-weight: 700;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 2rem;
}

.nav-links a {
  font-size: 0.95rem;
  font-weight: 500;
  color: var(--color-text-muted);
  transition: color var(--transition);
}

.nav-links a:hover {
  color: var(--color-text);
}

.nav-cta {
  background: var(--color-accent) !important;
  color: var(--color-bg) !important;
  padding: 0.6rem 1.25rem;
  border-radius: 8px;
  font-weight: 600 !important;
}

.nav-cta:hover {
  background: var(--color-accent-hover) !important;
}

.nav-toggle {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
}

.nav-toggle span {
  display: block;
  width: 24px;
  height: 2px;
  background: var(--color-text);
  transition: var(--transition);
}

/* Hero */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding: 8rem 0 4rem;
}

.hero-bg {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(135deg, rgba(13, 15, 18, 0.88) 0%, rgba(13, 15, 18, 0.72) 50%, rgba(13, 15, 18, 0.92) 100%),
    radial-gradient(ellipse 80% 60% at 70% 20%, rgba(245, 158, 11, 0.10) 0%, transparent 60%),
    url("../images/garage-storefront.jpg");
  background-size: cover;
  background-position: center 35%;
  background-color: var(--color-bg);
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, transparent 60%, var(--color-bg) 100%);
}

.hero-content {
  position: relative;
  z-index: 1;
}

.hero-tag {
  display: inline-block;
  font-size: 0.85rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--color-accent);
  margin-bottom: 1rem;
  padding: 0.4rem 1rem;
  background: var(--color-accent-muted);
  border-radius: 100px;
  border: 1px solid rgba(245, 158, 11, 0.25);
}

.hero h1 {
  font-family: var(--font-display);
  font-size: clamp(3rem, 8vw, 5.5rem);
  line-height: 1;
  letter-spacing: 0.02em;
  margin-bottom: 1.5rem;
}

.hero h1 .highlight {
  color: var(--color-accent);
}

.hero-desc {
  font-size: 1.15rem;
  color: var(--color-text-muted);
  max-width: 540px;
  margin-bottom: 2rem;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  margin-bottom: 4rem;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.9rem 1.75rem;
  font-family: var(--font-body);
  font-size: 1rem;
  font-weight: 600;
  border-radius: 10px;
  border: 2px solid transparent;
  cursor: pointer;
  transition: all var(--transition);
}

.btn-primary {
  background: var(--color-accent);
  color: var(--color-bg);
}

.btn-primary:hover {
  background: var(--color-accent-hover);
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(245, 158, 11, 0.3);
}

.btn-outline {
  background: transparent;
  color: var(--color-text);
  border-color: var(--color-border);
}

.btn-outline:hover {
  border-color: var(--color-accent);
  color: var(--color-accent);
}

.btn-full {
  width: 100%;
}

.hero-stats {
  display: flex;
  flex-wrap: wrap;
  gap: 3rem;
  padding-top: 2rem;
  border-top: 1px solid var(--color-border);
}

.stat-num {
  display: block;
  font-family: var(--font-display);
  font-size: 2.5rem;
  color: var(--color-accent);
  line-height: 1;
}

.stat-label {
  font-size: 0.9rem;
  color: var(--color-text-muted);
}

/* Sections */
.section {
  padding: 6rem 0;
}

.section-header {
  text-align: center;
  max-width: 600px;
  margin: 0 auto 3.5rem;
}

.section-tag {
  display: inline-block;
  font-size: 0.8rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--color-accent);
  margin-bottom: 0.75rem;
}

.section-header h2 {
  font-family: var(--font-display);
  font-size: clamp(2.25rem, 5vw, 3.25rem);
  letter-spacing: 0.02em;
  margin-bottom: 1rem;
}

.section-header p {
  color: var(--color-text-muted);
}

/* Services */
.services {
  background: var(--color-bg-elevated);
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.service-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  transition: transform var(--transition), border-color var(--transition), box-shadow var(--transition);
}

.service-card:hover {
  transform: translateY(-4px);
  border-color: rgba(245, 158, 11, 0.3);
  box-shadow: var(--shadow);
}

.service-card-media {
  position: relative;
  height: 170px;
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  overflow: hidden;
}

.service-card-media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.service-card-icon {
  position: absolute;
  left: 1.5rem;
  bottom: -28px;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  box-shadow: var(--shadow);
  z-index: 2;
}

.service-card-body {
  padding: 2.75rem 1.75rem 1.75rem;
}

.service-card-eyebrow {
  display: block;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-accent);
  margin-bottom: 0.5rem;
}

.service-card h3 {
  font-size: 1.25rem;
  margin-bottom: 0.75rem;
}

.service-card p {
  color: var(--color-text-muted);
  font-size: 0.95rem;
  margin-bottom: 1.25rem;
}

.service-card-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--color-text);
  border: 1px solid var(--color-border);
  padding: 0.6rem 1.1rem;
  border-radius: 8px;
  transition: border-color var(--transition), color var(--transition);
}

.service-card-link:hover {
  border-color: var(--color-accent);
  color: var(--color-accent);
}

.service-card-link .arrow {
  transition: transform var(--transition);
}

.service-card-link:hover .arrow {
  transform: translateX(3px);
}

/* About */
.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
}

.about-image {
  position: relative;
  aspect-ratio: 4/5;
  background:
    linear-gradient(145deg, var(--color-surface) 0%, var(--color-bg-card) 100%);
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-border);
  overflow: hidden;
}

.about-image::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    repeating-linear-gradient(
      -45deg,
      transparent,
      transparent 20px,
      rgba(245, 158, 11, 0.03) 20px,
      rgba(245, 158, 11, 0.03) 40px
    );
}

.about-image::after {
  content: "🔩";
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 5rem;
  opacity: 0.3;
}

.about-badge {
  position: absolute;
  bottom: 1.5rem;
  right: 1.5rem;
  background: var(--color-accent);
  color: var(--color-bg);
  padding: 1.25rem 1.5rem;
  border-radius: var(--radius);
  text-align: center;
}

.badge-num {
  display: block;
  font-family: var(--font-display);
  font-size: 2.5rem;
  line-height: 1;
}

.badge-text {
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  line-height: 1.3;
}

.about-content h2 {
  font-family: var(--font-display);
  font-size: clamp(2rem, 4vw, 2.75rem);
  letter-spacing: 0.02em;
  margin-bottom: 1.25rem;
}

.about-content p {
  color: var(--color-text-muted);
  margin-bottom: 1rem;
}

.about-features {
  margin-top: 1.5rem;
}

.about-features li {
  position: relative;
  padding-left: 1.75rem;
  margin-bottom: 0.75rem;
  color: var(--color-text-muted);
}

.about-features li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--color-accent);
  font-weight: 700;
}

/* Pricing */
.pricing {
  background: var(--color-bg);
}

.pricing-table-wrap {
  overflow-x: auto;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg-card);
}

.pricing-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.98rem;
}

.pricing-table th,
.pricing-table td {
  padding: 1rem 1.25rem;
  text-align: left;
  border-bottom: 1px solid var(--color-border);
}

.pricing-table th {
  background: var(--color-surface);
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--color-text-muted);
}

.pricing-table tbody tr:last-child td {
  border-bottom: none;
}

.pricing-table td:last-child {
  color: var(--color-accent);
  white-space: nowrap;
}

.pricing-table td strong {
  color: var(--color-accent-hover);
}

.pricing-note {
  margin-top: 1.25rem;
  text-align: center;
  font-size: 0.9rem;
  color: var(--color-text-muted);
}

/* Why Us */
.why-us {
  background: var(--color-bg-elevated);
}

.why-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 1.5rem;
}

.why-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2rem;
  position: relative;
  overflow: hidden;
}

.why-card::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 4px;
  height: 100%;
  background: var(--color-accent);
  opacity: 0;
  transition: opacity var(--transition);
}

.why-card:hover::before {
  opacity: 1;
}

.why-num {
  font-family: var(--font-display);
  font-size: 3rem;
  color: rgba(245, 158, 11, 0.2);
  line-height: 1;
  display: block;
  margin-bottom: 0.5rem;
}

.why-card h3 {
  font-size: 1.15rem;
  margin-bottom: 0.75rem;
}

.why-card p {
  color: var(--color-text-muted);
  font-size: 0.95rem;
}

/* Testimonials */
.testimonials-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.testimonial {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2rem;
}

.testimonial p {
  font-size: 1.05rem;
  font-style: italic;
  color: var(--color-text-muted);
  margin-bottom: 1.5rem;
  line-height: 1.7;
}

.testimonial p::before {
  content: "\201C";
  color: var(--color-accent);
  font-size: 2rem;
  line-height: 0;
  vertical-align: -0.4em;
  margin-right: 0.15em;
}

.testimonial footer {
  display: flex;
  flex-direction: column;
  gap: 0.15rem;
}

.testimonial footer strong {
  color: var(--color-text);
}

.testimonial footer span {
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

/* Contact */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

.contact-info h2 {
  font-family: var(--font-display);
  font-size: clamp(2rem, 4vw, 2.75rem);
  letter-spacing: 0.02em;
  margin-bottom: 1rem;
}

.contact-info > p {
  color: var(--color-text-muted);
  margin-bottom: 2rem;
}

.contact-details li {
  display: flex;
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.contact-icon {
  font-size: 1.25rem;
  flex-shrink: 0;
}

.contact-details strong {
  display: block;
  font-size: 0.85rem;
  color: var(--color-text-muted);
  margin-bottom: 0.15rem;
}

.contact-details a {
  color: var(--color-accent);
  transition: color var(--transition);
}

.contact-details a:hover {
  color: var(--color-accent-hover);
}

.contact-form {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2rem;
}

.contact-form h3 {
  font-size: 1.25rem;
  margin-bottom: 1.5rem;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}

.contact-form label {
  display: block;
  margin-bottom: 1rem;
}

.contact-form label span {
  display: block;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--color-text-muted);
  margin-bottom: 0.4rem;
}

.contact-form input,
.contact-form select,
.contact-form textarea {
  width: 100%;
  padding: 0.75rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  color: var(--color-text);
  transition: border-color var(--transition);
}

.contact-form input:focus,
.contact-form select:focus,
.contact-form textarea:focus {
  outline: none;
  border-color: var(--color-accent);
}

.contact-form textarea {
  resize: vertical;
  min-height: 100px;
}

.form-note {
  margin-top: 1rem;
  font-size: 0.9rem;
  text-align: center;
  min-height: 1.4em;
}

.form-note.success {
  color: #4ade80;
}

.form-note.error {
  color: #f87171;
}

/* Footer */
.footer {
  background: var(--color-bg-elevated);
  border-top: 1px solid var(--color-border);
  padding: 4rem 0 0;
}

.footer-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 3rem;
  padding-bottom: 3rem;
}

.footer-brand p {
  color: var(--color-text-muted);
  font-size: 0.95rem;
  margin-top: 1rem;
  max-width: 320px;
}

.footer-links h4 {
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-text-muted);
  margin-bottom: 1rem;
}

.footer-links li {
  margin-bottom: 0.5rem;
}

.footer-links a {
  color: var(--color-text-muted);
  font-size: 0.95rem;
  transition: color var(--transition);
}

.footer-links a:hover {
  color: var(--color-accent);
}

.footer-bottom {
  padding: 1.5rem 0;
  border-top: 1px solid var(--color-border);
  text-align: center;
}

.footer-bottom p {
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

/* Mobile */
@media (max-width: 768px) {
  .nav-toggle {
    display: flex;
  }

  .nav-links {
    position: fixed;
    top: 0;
    right: -100%;
    width: min(300px, 85vw);
    height: 100vh;
    flex-direction: column;
    align-items: flex-start;
    padding: 5rem 2rem 2rem;
    background: var(--color-bg-elevated);
    border-left: 1px solid var(--color-border);
    transition: right var(--transition);
    gap: 1.5rem;
  }

  .nav-links.open {
    right: 0;
  }

  .nav-toggle.open span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  .nav-toggle.open span:nth-child(2) {
    opacity: 0;
  }

  .nav-toggle.open span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
  }

  .about-grid,
  .contact-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  .about-visual {
    order: -1;
  }

  .about-image {
    aspect-ratio: 16/10;
  }

  .form-row {
    grid-template-columns: 1fr;
  }

  .footer-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .hero-stats {
    gap: 2rem;
  }
}

/* ============================= */
/* Inventory Page                */
/* ============================= */

.inventory-hero {
  padding: 9rem 0 2.5rem;
  background: linear-gradient(180deg, var(--color-bg-elevated) 0%, var(--color-bg) 100%);
  border-bottom: 1px solid var(--color-border);
}

.inventory-hero .section-tag {
  margin-bottom: 0.75rem;
}

.inventory-hero h1 {
  font-family: var(--font-display);
  font-size: clamp(2.25rem, 5vw, 3.25rem);
  letter-spacing: 0.02em;
  margin-bottom: 0.75rem;
}

.inventory-hero p {
  color: var(--color-text-muted);
  max-width: 560px;
}

.inventory-main {
  padding: 3rem 0 6rem;
}

.inv-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.25rem;
  margin-bottom: 2.5rem;
}

.inv-stat-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
}

.inv-stat-card .inv-stat-num {
  font-family: var(--font-display);
  font-size: 2.25rem;
  color: var(--color-accent);
  line-height: 1;
  display: block;
}

.inv-stat-card.low .inv-stat-num {
  color: #f87171;
}

.inv-stat-card .inv-stat-label {
  font-size: 0.85rem;
  color: var(--color-text-muted);
  margin-top: 0.4rem;
  display: block;
}

.inv-toolbar {
  display: flex;
  flex-wrap: wrap;
  gap: 0.85rem;
  align-items: center;
  margin-bottom: 1.5rem;
}

.inv-toolbar input[type="text"],
.inv-toolbar select {
  padding: 0.75rem 1rem;
  font-family: var(--font-body);
  font-size: 0.95rem;
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  color: var(--color-text);
}

.inv-toolbar input[type="text"] {
  flex: 1;
  min-width: 220px;
}

.inv-toolbar input:focus,
.inv-toolbar select:focus {
  outline: none;
  border-color: var(--color-accent);
}

.inv-table-wrap {
  overflow-x: auto;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg-card);
}

.inv-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.92rem;
  min-width: 760px;
}

.inv-table th,
.inv-table td {
  padding: 1rem 1.1rem;
  text-align: left;
  border-bottom: 1px solid var(--color-border);
  vertical-align: middle;
}

.inv-table th {
  background: var(--color-surface);
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--color-text-muted);
  white-space: nowrap;
}

.inv-table tbody tr:last-child td {
  border-bottom: none;
}

.inv-table tbody tr:hover {
  background: rgba(255, 255, 255, 0.02);
}

.inv-part-name {
  font-weight: 600;
  color: var(--color-text);
}

.inv-status {
  display: inline-block;
  padding: 0.3rem 0.7rem;
  border-radius: 100px;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.inv-status--ok {
  background: rgba(74, 222, 128, 0.12);
  color: #4ade80;
}

.inv-status--low {
  background: rgba(248, 113, 113, 0.12);
  color: #f87171;
}

.inv-row-actions {
  display: flex;
  gap: 0.5rem;
  white-space: nowrap;
}

.inv-action-btn {
  padding: 0.4rem 0.7rem;
  font-size: 0.78rem;
  font-family: var(--font-body);
  background: transparent;
  border: 1px solid var(--color-border);
  border-radius: 6px;
  color: var(--color-text-muted);
  cursor: pointer;
  transition: border-color var(--transition), color var(--transition);
}

.inv-action-btn:hover {
  border-color: var(--color-accent);
  color: var(--color-accent);
}

.inv-action-btn.danger:hover {
  border-color: #f87171;
  color: #f87171;
}

.inv-action-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}

.inv-empty {
  text-align: center;
  padding: 3rem 1rem;
  color: var(--color-text-muted);
}

.inv-note {
  margin-top: 1.25rem;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

/* Modal */
.inv-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(13, 15, 18, 0.75);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 2000;
  padding: 1.5rem;
}

.inv-modal-overlay.open {
  display: flex;
}

.inv-modal {
  width: 100%;
  max-width: 440px;
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2rem;
}

.inv-modal h3 {
  font-size: 1.25rem;
  margin-bottom: 1.25rem;
}

.inv-modal label {
  display: block;
  margin-bottom: 1rem;
}

.inv-modal label span {
  display: block;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--color-text-muted);
  margin-bottom: 0.4rem;
}

.inv-modal input,
.inv-modal select {
  width: 100%;
  padding: 0.75rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  color: var(--color-text);
}

.inv-modal input:focus,
.inv-modal select:focus {
  outline: none;
  border-color: var(--color-accent);
}

.inv-modal-actions {
  display: flex;
  gap: 0.85rem;
  margin-top: 1.5rem;
}

.inv-modal-actions .btn {
  flex: 1;
}

@media (max-width: 768px) {
  .inv-stats {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* ============================= */
/* Auth (Login / Register)       */
/* ============================= */

.auth-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8rem 1.5rem 4rem;
}

.auth-card {
  width: 100%;
  max-width: 440px;
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2.5rem;
}

.auth-card .section-tag {
  display: block;
  margin-bottom: 0.5rem;
}

.auth-card h1 {
  font-family: var(--font-display);
  font-size: 2rem;
  letter-spacing: 0.02em;
  margin-bottom: 0.5rem;
}

.auth-card > p.auth-sub {
  color: var(--color-text-muted);
  font-size: 0.92rem;
  margin-bottom: 1.75rem;
}

.auth-card label {
  display: block;
  margin-bottom: 1.1rem;
}

.auth-card label span {
  display: block;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--color-text-muted);
  margin-bottom: 0.4rem;
}

.auth-card input,
.auth-card select {
  width: 100%;
  padding: 0.75rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  color: var(--color-text);
}

.auth-card input:focus,
.auth-card select:focus {
  outline: none;
  border-color: var(--color-accent);
}

.auth-error {
  background: rgba(248, 113, 113, 0.1);
  border: 1px solid rgba(248, 113, 113, 0.3);
  color: #f87171;
  font-size: 0.88rem;
  padding: 0.75rem 1rem;
  border-radius: 8px;
  margin-bottom: 1.25rem;
  display: none;
}

.auth-error.visible {
  display: block;
}

.auth-demo-box {
  margin-top: 1.75rem;
  padding: 1rem 1.1rem;
  background: var(--color-surface);
  border-radius: 8px;
  font-size: 0.82rem;
  color: var(--color-text-muted);
  line-height: 1.6;
}

.auth-demo-box strong {
  color: var(--color-text);
}

.auth-switch {
  text-align: center;
  margin-top: 1.75rem;
  font-size: 0.9rem;
  color: var(--color-text-muted);
}

.auth-switch a {
  color: var(--color-accent);
  font-weight: 600;
}

.auth-switch a:hover {
  color: var(--color-accent-hover);
}

/* Auth-aware nav slot */
.nav-user-pill {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

.nav-user-pill strong {
  color: var(--color-text);
}

.nav-logout-link {
  color: var(--color-text-muted) !important;
}

.nav-logout-link:hover {
  color: #f87171 !important;
}

/* ============================= */
/* Dashboards (shared)            */
/* ============================= */

.dash-hero {
  padding: 9rem 0 2rem;
  background: linear-gradient(180deg, var(--color-bg-elevated) 0%, var(--color-bg) 100%);
  border-bottom: 1px solid var(--color-border);
}

.dash-hero h1 {
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3rem);
  letter-spacing: 0.02em;
  margin-bottom: 0.4rem;
}

.dash-hero p {
  color: var(--color-text-muted);
}

.dash-main {
  padding: 2.5rem 0 6rem;
}

.dash-tabs {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 2rem;
  border-bottom: 1px solid var(--color-border);
}

.dash-tab {
  padding: 0.85rem 1.4rem;
  font-family: var(--font-body);
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--color-text-muted);
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  cursor: pointer;
  transition: color var(--transition), border-color var(--transition);
}

.dash-tab:hover {
  color: var(--color-text);
}

.dash-tab.active {
  color: var(--color-accent);
  border-bottom-color: var(--color-accent);
}

.dash-panel {
  display: none;
}

.dash-panel.active {
  display: block;
}

/* Booking form (customer) */
.booking-form-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 2rem;
  max-width: 560px;
}

.booking-form-card label {
  display: block;
  margin-bottom: 1.1rem;
}

.booking-form-card label span {
  display: block;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--color-text-muted);
  margin-bottom: 0.4rem;
}

.booking-form-card input,
.booking-form-card select,
.booking-form-card textarea {
  width: 100%;
  padding: 0.75rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  color: var(--color-text);
}

.booking-form-card input:focus,
.booking-form-card select:focus,
.booking-form-card textarea:focus {
  outline: none;
  border-color: var(--color-accent);
}

.booking-form-card textarea {
  resize: vertical;
  min-height: 90px;
}

/* Bookings table (shared by staff + customer views) */
.bookings-table-wrap {
  overflow-x: auto;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg-card);
}

.bookings-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.92rem;
  min-width: 700px;
}

.bookings-table th,
.bookings-table td {
  padding: 1rem 1.1rem;
  text-align: left;
  border-bottom: 1px solid var(--color-border);
  vertical-align: middle;
}

.bookings-table th {
  background: var(--color-surface);
  font-size: 0.78rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--color-text-muted);
  white-space: nowrap;
}

.bookings-table tbody tr:last-child td {
  border-bottom: none;
}

.bookings-table select.status-select {
  padding: 0.4rem 0.6rem;
  font-size: 0.82rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 6px;
  color: var(--color-text);
}

.booking-status {
  display: inline-block;
  padding: 0.3rem 0.7rem;
  border-radius: 100px;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.booking-status--pending {
  background: rgba(245, 158, 11, 0.12);
  color: var(--color-accent);
}

.booking-status--confirmed {
  background: rgba(96, 165, 250, 0.12);
  color: #60a5fa;
}

.booking-status--completed {
  background: rgba(74, 222, 128, 0.12);
  color: #4ade80;
}

.booking-status--cancelled {
  background: rgba(248, 113, 113, 0.12);
  color: #f87171;
}

.dash-empty {
  text-align: center;
  padding: 3rem 1rem;
  color: var(--color-text-muted);
}

.dash-note {
  margin-top: 1.25rem;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

.dash-role-badge {
  display: inline-block;
  padding: 0.25rem 0.65rem;
  border-radius: 100px;
  background: var(--color-accent-muted);
  color: var(--color-accent);
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-left: 0.6rem;
  vertical-align: middle;
}

/* ============================= */
/* Gallery Carousel               */
/* ============================= */

.bay-gallery {
  background: var(--color-bg-elevated);
  padding: 6rem 0;
}

.bay-gallery .section-header {
  margin-bottom: 2.5rem;
}

.carousel {
  position: relative;
}

.carousel-viewport {
  overflow: hidden;
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow);
}

.carousel-track {
  display: flex;
  transition: transform 0.5s ease;
  will-change: transform;
}

.carousel-slide {
  position: relative;
  flex: 0 0 100%;
  aspect-ratio: 16 / 8;
  background: var(--color-bg-card);
}

.carousel-slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.carousel-caption {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 2.5rem 2rem 1.75rem;
  background: linear-gradient(to top, rgba(13, 15, 18, 0.92) 0%, rgba(13, 15, 18, 0.5) 55%, transparent 100%);
}

.carousel-caption-tag {
  display: inline-block;
  font-size: 0.78rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-accent);
  margin-bottom: 0.4rem;
}

.carousel-caption p {
  font-size: 1rem;
  max-width: 520px;
  color: var(--color-text);
}

.carousel-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 46px;
  height: 46px;
  border-radius: 50%;
  background: rgba(13, 15, 18, 0.65);
  border: 1px solid var(--color-border);
  color: var(--color-text);
  font-size: 1.6rem;
  line-height: 1;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background var(--transition), border-color var(--transition), transform var(--transition);
  z-index: 2;
}

.carousel-btn:hover {
  background: var(--color-accent);
  color: var(--color-bg);
  border-color: var(--color-accent);
}

.carousel-btn--prev {
  left: 1rem;
}

.carousel-btn--next {
  right: 1rem;
}

.carousel-dots {
  display: flex;
  justify-content: center;
  gap: 0.6rem;
  margin-top: 1.5rem;
}

.carousel-dot {
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: var(--color-border);
  border: none;
  cursor: pointer;
  padding: 0;
  transition: background var(--transition), transform var(--transition);
}

.carousel-dot.active {
  background: var(--color-accent);
  transform: scale(1.25);
}

@media (max-width: 768px) {
  .carousel-slide {
    aspect-ratio: 4 / 5;
  }

  .carousel-caption {
    padding: 2rem 1.25rem 1.25rem;
  }

  .carousel-btn {
    width: 38px;
    height: 38px;
    font-size: 1.3rem;
  }

  .carousel-btn--prev {
    left: 0.5rem;
  }

  .carousel-btn--next {
    right: 0.5rem;
  }
}

/* ============================= */
/* Form Validation (booking form) */
/* ============================= */

.field-error {
  display: block;
  min-height: 1.1em;
  font-size: 0.8rem;
  color: #f87171;
  margin-top: 0.35rem;
}

.booking-form-card input.invalid,
.booking-form-card select.invalid {
  border-color: #f87171 !important;
  box-shadow: 0 0 0 1px rgba(248, 113, 113, 0.25);
}

.booking-form-card input.valid,
.booking-form-card select.valid {
  border-color: #4ade80 !important;
}

/* ============================= */
/* Disabled submit state          */
/* ============================= */

.btn-disabled,
.btn:disabled {
  opacity: 0.45;
  cursor: not-allowed;
  pointer-events: none;
}

/* ============================= */
/* Cart                           */
/* ============================= */

.cart-modal {
  max-width: 640px;
  width: 100%;
}

.cart-table-wrap {
  max-height: 320px;
  overflow-y: auto;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  margin-bottom: 1.25rem;
}

.cart-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.9rem;
}

.cart-table th,
.cart-table td {
  padding: 0.85rem 1rem;
  text-align: left;
  border-bottom: 1px solid var(--color-border);
}

.cart-table th {
  background: var(--color-surface);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
  position: sticky;
  top: 0;
}

.cart-table tbody tr:last-child td {
  border-bottom: none;
}

.cart-qty-input {
  width: 64px;
  padding: 0.4rem 0.5rem;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 6px;
  color: var(--color-text);
  font-family: var(--font-body);
  font-size: 0.9rem;
}

.cart-qty-input:focus {
  outline: none;
  border-color: var(--color-accent);
}

.cart-total-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-family: var(--font-display);
  font-size: 1.25rem;
  letter-spacing: 0.02em;
  padding: 0.75rem 0;
  border-top: 1px solid var(--color-border);
  margin-bottom: 0.5rem;
}

.cart-total-row span:last-child {
  color: var(--color-accent);
}

/* ============================= */
/* Receipts                       */
/* ============================= */

.booking-status--paid {
  background: rgba(74, 222, 128, 0.12);
  color: #4ade80;
}

.receipt-modal {
  max-width: 520px;
  width: 100%;
}

.receipt-header {
  display: flex;
  align-items: center;
  gap: 0.85rem;
  margin-bottom: 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 1px solid var(--color-border);
}

.receipt-logo {
  width: 44px;
  height: 44px;
  object-fit: contain;
  background: #fff;
  border-radius: 8px;
  padding: 3px;
  flex-shrink: 0;
}

.receipt-meta {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.receipt-meta div {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.receipt-meta span {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
}

.receipt-items-table {
  margin-bottom: 1.25rem;
}

@media print {
  body * {
    visibility: hidden;
  }
  #receiptPrintArea,
  #receiptPrintArea * {
    visibility: visible;
  }
  #receiptPrintArea {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    box-shadow: none;
    border: none;
  }
  .no-print {
    display: none !important;
  }
}

</style>